import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.recipes.app',
  appName: 'ionic-angular-course',
  webDir: 'www',
};

export default config;
